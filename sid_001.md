Project CI/CD architecture
Explain me about recently created ci/cd ?
How you are deploying to the production eks clusters ?
Diff b/w deployments, daemonsets & statefulsets ?
what happens if kube-proxy pod crashes on a worker node ?
How ansible integrated with EKS cluster ?
How docker images are pushing to ECR ?
How helm commands are executing on EKS cluster nodes ? where does these helm commands are running actually ?
How Jenkins is playing as a deployment job ?
How Prometheus & Grafana deployed ?
How Prometheus & Grafana are integrated ?
How do u handle different eks cluster within a single vpc ? explain it ?
What is the default add-on while creating eks cluster?
How do you disable vpc cni ?
What is the network cni you are using ? #Calico

For what resources you are using terraform ?
How modules are using in your terraform code ?
what is ELK is used for ?
