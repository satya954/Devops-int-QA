rename the files A1.txt to B1.txt
find files with creation time more than 7 days
find files which are modified today.
replace the keyword 'global' to 'network' among the files
print odd numebrs in a shell script

kubernetes upgrade activity ( Manually ? )
wht not with terraform
kubernetes nodeaffinitity
podAffinity
taints & tolerations
kubernetes dashboard any ?
How many clusters.
How do you access all these clusters.
kubernetes nodes - high disk utilization which docker process is causing the issue.
OOM killed - error 137 => how do you troubleshoot it.
	describe the events
	steps and the commands to execute it

secrets are stored in 
. AWS Secrets manager - if expired how do you know about it - what will you do ( create anonther or what )
. Deployment pods will unable to start - how will you get to notify about it.

Rancher RBAC
Rancher (RKE2)

Prometheus how integrate
version using
Alertmanager - how sending alerts
Disk utilization among the nodes 

ingress types ( nginx )
HOW ALB are using ?

S3 lifecycle - which you are using now ?
Are you using any glacier ?

terraform plan ( means )
what will happen if without running plan command, apply command runs 
manually changes done in the aws, how do resolve it 
terraform plan refresh ( command means )
terraform plan --refresh-only
terraform state file (how to load in your local )
terraform migrate state how 
terraform lifecycle ( create-before-destroy ) when to use.
revert to older version.
terraform state lost - how do you recover the situation.
terraform internal moduels how will save

git branching strategy
git squash
what will do with the branch after merging to main branch?

npm rc file configuration where 
jenkins - deployment stages
how ansible is integrated
Ansible Tower & AWS Tower ?
How do u manage infra ?
How ELK where deployed ?

Diff b/w ansible and chef ( Major differences )

Can i attach same EBS volumes to diff ec2 instances ?

ALB - types
ALB refresh time period - meantime suddent spike what will happen
AWS - ec2 instances deployed by you. types
sendEmail / mailx
VPC peering how ?

Database slow query log - how will you handle them.
RDS - MySQL version - what you do in it ?
 replication
 user access.
 
redis - ttl ( and default )
how to get to know that a key is there or not in redis.
what type of instance is using for redis. How will handle with 10GB of memory ?
which memory type is using for redis

Total architecture flow of your project.

